C# console application that determines if the ticket number entered by the user is lucky.

## Local run
`run.bat` – the file with `dotnet run` command that starts compilation and execution of the program